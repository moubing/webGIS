import { createContext } from "react";

// 存储搜索记录
export const RecordsContext = createContext(null);
export const SetRecordsContext = createContext(null);

// 存储查询结果
export const ResultsContext = createContext(null);
export const SetResultsContext = createContext(null);
