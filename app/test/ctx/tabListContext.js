import { createContext } from "react";

export const TabListContext = createContext(null);
export const SetTabListContext = createContext(null);

export const ToolTabListContext = createContext(null);
export const SetToolTabListContext = createContext(null);
