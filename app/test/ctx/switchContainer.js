import { createContext } from "react";

// switch on/off log container
export const LogConCtx = createContext(null);
export const SetLogConCtx = createContext(null);

// switch on/off tool container
export const ToolConCtx = createContext(null);
export const SetToolConCtx = createContext(null);

// switch on/off tip container
export const TipConCtx = createContext(null);
export const SetTipConCtx = createContext(null);
